services:
  - type: web
    name: gst-billing
    env: php
    buildCommand: "composer install --no-dev"
    startCommand: "php artisan serve --host 0.0.0.0 --port 10000"
    region: oregon
    plan: free
